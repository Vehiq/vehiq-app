# VEHIQ backend — Python 3.11 — direct dependencies only
# (transitive deps are resolved by pip; pinned exact versions for reproducibility)

# Web framework
fastapi==0.110.1
uvicorn==0.25.0
python-multipart==0.0.24
pydantic==2.12.5
email-validator==2.3.0

# Database (MongoDB Atlas via Motor async + certifi for TLS)
motor==3.3.1
pymongo==4.5.0
certifi==2026.2.25

# Auth / crypto
PyJWT==2.12.1
bcrypt==4.1.3

# Email (Brevo SMTP)
aiosmtplib==5.1.0

# HTTP clients (Google OAuth lookup, generic)
httpx==0.28.1

# AI Mechanic — Claude Sonnet 4.5
anthropic==0.100.0

# Object storage (Cloudflare R2 via S3 API)
boto3==1.42.86

# Image processing (WebP full + thumbnail)
pillow==12.2.0

# Env loading
python-dotenv==1.2.2

# Dev / test
pytest==9.0.3
requests==2.33.1
